# Aamir Portfolio Site

Simple static portfolio website (HTML/CSS/JS).

## Run locally
```bash
cd portfolio-site
python3 -m http.server 8088
```
Then open http://localhost:8088

## Deployment Notes
- Hosted with GitHub Pages from `main` branch (`/` root).
- If updates don’t show instantly, wait 1-2 minutes and hard refresh.

